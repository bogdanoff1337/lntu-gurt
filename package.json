{
  "name": "lntu-gurt",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "control:dev": "npm run dev --prefix resources/control",
    "control:build": "npm run build --prefix resources/control",
    "main:dev": "npm run start:vite --prefix resources/main",
    "main:build": "npm build:vite --prefix resources/main"
  }
}
